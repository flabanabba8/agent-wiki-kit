# Generated by scripts/agents/sync.py from .mcp.json. Edit that file, then rerun.
# Codex loads this only after you trust the project, and its hooks (.codex/hooks.json) only
# after you review them once with /hooks.

[mcp_servers.mem0-hot]
command = "bash"
args = ["-c", "cd \"$(git rev-parse --show-toplevel)\" && exec ./.venv/bin/python scripts/mem0/mcp_server.py"]
startup_timeout_sec = 60

[mcp_servers.camoufox]
command = "bash"
args = ["-c", "cd \"$(git rev-parse --show-toplevel)\" && exec ./.venv/bin/python scripts/camoufox/mcp_server.py"]
startup_timeout_sec = 60
